document.getElementById('registration-form').addEventListener('submit', function(event) {
    event.preventDefault();
    // Vous pouvez ajouter ici le code pour traiter les données du formulaire, comme envoyer une requête au serveur.
  });
  